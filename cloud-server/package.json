{
  "name": "media-cloud-server",
  "version": "1.0.0",
  "description": "Server to uploading media to Cloudinary, Closedmind API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "heroku-config": "heroku git:remote -a closedmind-cloudinary-api",
    "heroku-deploy": "git push heroku master"
  },
  "author": "leflores",
  "license": "MIT",
  "dependencies": {
    "cloudinary": "^1.29.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "express-fileupload": "^1.3.1",
    "fs-extra": "^10.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
